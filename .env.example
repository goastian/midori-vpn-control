# URL base del backend API (vacío para usar mismo host/origen)
VITE_API_URL=

# Authentik OAuth2/OIDC
# Ejemplo: https://auth.example.com/application/o/tu-app/
VITE_AUTHENTIK_ISSUER=REPLACE_WITH_AUTHENTIK_ISSUER_URL
VITE_AUTHENTIK_CLIENT_ID=REPLACE_WITH_AUTHENTIK_CLIENT_ID
# Vacío = usar automáticamente ${window.location.origin}/auth/callback
VITE_AUTHENTIK_REDIRECT_URI=
